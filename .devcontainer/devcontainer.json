// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-existing-dockerfile
{
	"name": "ros2_humble",
	"build": {
		"context": "..",
		"dockerfile": "../.docker/Dockerfile"
	},
	
	"runArgs": ["--name", "ros2_humble_gazebo_kuka_iiwa", "-e", "DISPLAY=host.docker.internal:0.0"]

	//, "postCreateCommand": "rosdep update && rosdep install -y --from-paths /workspace/src --ignore-src --rosdistro humble"

	//, "postCreateCommand": "cd /workspaces/ros2_gazebo_kuka_iiwa/ros2_ws && colcon build"
}
